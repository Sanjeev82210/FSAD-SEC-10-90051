# Copilot Instructions

- This workspace contains a Spring Boot JPA demo named `trainingdb1`.
- Keep changes focused on the existing Spring Boot structure.
- Prefer simple, buildable Java code and minimal dependencies.
- Use H2 for local persistence unless the user asks otherwise.
- Validate changes with Maven after code edits.
